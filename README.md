## Configuration & Environment Variables

The application requires several environment variables to be set for database connectivity and caching.

### Local Setup

1. Copy the template environment file to create your local `.env` file:
   ```bash
   cp .env.example .env