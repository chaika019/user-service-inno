spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: chaika
    password: '123'
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8081